import { TurnContext, TurnState, Storage, UserTokenClient } from '@microsoft/agents-hosting';
export declare class TeamsOAuthFlowAppStyle {
    userTokenClient?: UserTokenClient;
    tokenExchangeId: string | null;
    storage: Storage;
    appState: TurnState | null;
    constructor(storage: Storage);
    beginFlow(context: TurnContext, state: TurnState): Promise<string>;
    continueFlow(context: TurnContext): Promise<"" | undefined>;
    signOut(context: TurnContext): Promise<void>;
    userSignedInToken(context: TurnContext): Promise<string | undefined>;
}
